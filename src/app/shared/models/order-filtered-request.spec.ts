import { OrderFilteredRequest } from './order-filtered-request';

describe('OrderFilteredRequest', () => {
  it('should create an instance', () => {
    expect(new OrderFilteredRequest()).toBeTruthy();
  });
});
