import { OrderFilteredResponse } from './order-filtered-response';

describe('OrderFilteredResponse', () => {
  it('should create an instance', () => {
    expect(new OrderFilteredResponse()).toBeTruthy();
  });
});
