import { PlanReportRequest } from './plan-report-request';

describe('PlanReportRequest', () => {
  it('should create an instance', () => {
    expect(new PlanReportRequest()).toBeTruthy();
  });
});
